# Recommended application examples.
#
# 每个示例均拆分为独立子项目，存放在各自的子目录中：
# - base_window:       基础窗口示例子项目（占位，当前输出 Hello World）
# - settings_example:  Settings 参考应用（组件库 + 可执行文件）

subdir('base_window')
subdir('settings_example')
